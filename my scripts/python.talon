python else if:'elif'
python print:'print(f"")'
python input:'input("")'
python for loop:"for x in range(0,10):"
function comment:'"""\n\nParameters: \nReturn: \n"""' 
short definition:"def "
python in it:"def __init__(self,):"
python right:'file.write(f"{}")'
cursor execute:'cursor.execute(\n"""\n\n""",'
database comment:"db.commit()"
python SQL lay out:'import sqlite3\ntry:\n\ndb = sqlite3.connect("book_db.db")\ncursor = db.cursor()\nexcept Exception as e:\ndb.rollback()\nraise e\nfinally:\n	db.close()\n	print("Connection to database closed.")'
python activate:'source venv/Scripts/activate'
Django layout:"{% extends 'shop/base.html' %}\n{% block title %}x{% endblock %}\n{% block content %}\n\n{% endblock %}"
create virtual environment:'python -m venv venv'
make requirements:'pip freeze > requirements.txt'
create requirements:'pip freeze > requirements.txt'
run server:"python manage.py runserver"